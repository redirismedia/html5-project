// Music model
var MusicModel = Backbone.Model.extend({
    defaults: {
        
    }
});

// MusicButton view
var MusicButtonView = Backbone.View.extend({
    initialize: function() {
        
    },

    render: function() {
        
    }
});